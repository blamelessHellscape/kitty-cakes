{
  "name": "kitty-cakes_server",
  "version": "1.0.0",
  "description": "A  server to handle commands from Twilio. This will be used to handle events and perform the appropriate actions to the WhiskerWatcher.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "The Q Balls",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "request": "^2.81.0",
    "twilio": "^3.84.1"
  },
  "devDependencies": {
    "in-memory-database": "^1.0.1",
    "nodemon": "^2.0.20"
  }
}
